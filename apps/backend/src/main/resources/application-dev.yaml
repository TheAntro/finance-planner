spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finance-planner
    username: myuser
    password: secret
  jpa:
    properties:
      hibernate:
        "[format_sql]": true
        "[highlight_sql]": true
        "[use_sql_comments]": true
logging:
  level:
    "[org.hibernate.SQL]": DEBUG
    "[org.hibernate.orm.jdbc.bind]": TRACE
